PORT="8080" # The port the server will listen on.
NODE_ENV="development" # The environment mode: test, development or production.
REQUESTS_PER_MINUTE="100" # Maximum number of requests allowed per minute (rate limiting).
POSTGRES_DATABASE_URL="POSTGRES_DATABASE_URL" # The PostgreSQL database URL with connection pooling.
POSTGRES_DATABASE_URL_NON_POOLING="POSTGRES_DATABASE_URL_NON_POOLING" # PostgreSQL URL without connection pooling.
JWT_SECRET="YOUR_SECRET" # Secret key used to sign JWT tokens.
SALT_ROUNDS="10" # Number of bcrypt salt rounds for hashing passwords.